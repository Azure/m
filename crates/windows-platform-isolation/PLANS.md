# PLANS — windows-platform-isolation

Tracks CHECKLIST.md files in this source-component and their status.

| Path to CHECKLIST.md | Status | Brief description | Design Notes |
|---|---|---|---|
| [CHECKLIST.md](CHECKLIST.md) | in progress | M1 pure safe core **complete** (see COMPLETED-CHECKLIST.md). M2+ roadmap drafted for tuning: M2 new `windows-text` safe crate + `windows-text-sys` unsafe leaf (D16 / Option B; binds `windows` per revised D1), M3 adopt it here, M4 C++ artifact loader, M5 live registry provider (own `-sys` leaf), M6 filesystem surface, M9 live filesystem provider (cross-component prerequisite for `windows-win32-shim`), M10 short-name (8.3 / `cAlternateFileName`) fidelity for live enumeration (D23; cross-component prerequisite for `windows-win32-shim` MW8), M7 async/threadpool, M8 **in-process web request/response handler surface** (safe `RequestHandler` + identity/journaling decorators; D25 off = pass-through; replaces the former out-of-process HWC outline, now deferred under D17; ABI realization handed to `windows-win32-shim` MW11/MW12), M11 **egress (network-client) isolation surface** **(done — realizes D27 "network is the majority of the surface"; D31)**: safe `EgressSurface` + the D25 mode stack as decorators (passthrough/redirect/buffer/replay/block/observe) with a `LiveEgress` WinHTTP `-sys` bottom; HTTP-only first pass, SOAP/WWSAPI reserved; lifecycle reassembly + `.pilcfg` `egress` + `winhttp.dll` alias handed to `windows-win32-shim` MW17/MW18 | [DESIGN-NOTES.md](DESIGN-NOTES.md) |
