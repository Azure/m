# Unresolved test failures — PIL

_None currently._

Record any pre-existing or deferred test failure here (test executable, symptom,
root cause, confirmation that it is pre-existing/unrelated, and status) before
committing work that leaves it failing.

